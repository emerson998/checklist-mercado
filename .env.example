# Opcional — so necessario se quiser login (Google/GitHub) e sincronizar
# a lista entre aparelhos. Sem essas variaveis, o app funciona 100% via
# localStorage (comportamento padrao).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
